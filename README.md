# Laptop Price Predictor 💻

A machine learning project that estimates the price of a laptop based on its specifications (Ram, CPU, Storage, etc.).

## 🔧 Technologies Used
* Python
* Scikit-Learn (Random Forest)
* Pandas & Numpy

## 🚀 How to Run
1. Clone the repository.
2. Install dependencies:
    pip install -r requirements.txt
3. Run the predictor:
    python test.py
## 📊 Model Performance
The model was trained on a dataset of 1,300+ laptops and uses a Random Forest Regressor pipeline handling categorical data automatically.